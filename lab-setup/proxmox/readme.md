proxmox setup 
- why QEMU agent => to allow proxmox to connect directly to the vm
- TPM => is small software valute. its useful for encryptions
- assign new user admin permsion in datacenter
TASKS:
  - make sure AES-NI is enabled in the proxmox BIOS settings

